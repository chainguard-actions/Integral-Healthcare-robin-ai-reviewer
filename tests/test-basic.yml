name: Test basic usage - action starts and runs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          GITHUB_TOKEN: ${{ github.token }}
          OPEN_AI_API_KEY: fake-openai-key-for-test
          gpt_model_name: gpt-4-turbo
          github_api_url: https://api.github.com

      - name: Assert action ran (not a startup/container failure)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "action outcome: $OUTCOME"
          # The action should either succeed (empty diff) or fail (API errors)
          # but NOT be 'skipped' — it must have actually run.
          if [ "$OUTCOME" = "skipped" ]; then
            echo "FAIL: action was skipped, it did not run at all"
            exit 1
          fi
          echo "PASS: action ran (outcome=$OUTCOME)"
